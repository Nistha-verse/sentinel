[package]
name = "payment-vault-insecure"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "27.0.0-rc.1"


[profile.release]
overflow-checks = true
opt-level = 2
codegen-units = 1
lto = true
panic = "abort"
strip = "symbols"

